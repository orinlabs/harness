"""Example sandbox-shipped helper (placeholder).

Real bundles ship helpers like an atomic, contract-validating po_flow
writer here. This file only exists so the example bundle exercises the
`target: sandbox` path end to end.
"""

print("example sandbox script")
