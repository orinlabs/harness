"""Example sandbox-shipped helper (placeholder).

Real bundles ship helpers like a leveling-sheet builder that normalizes
subcontractor bids into a comparable CSV here. This file only exists so
the example bundle exercises the `target: sandbox` path end to end.
"""

print("example sandbox script")
